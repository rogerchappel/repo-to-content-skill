{
  "name": "sample-tool",
  "version": "0.1.0",
  "description": "A sample local CLI with evidence-backed docs.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/index.js",
    "smoke": "node src/index.js --help"
  }
}
